# This module reads an element library into a single data structure
# This module assumes no blank lines or comments in the library

import re

file = open('elelib.std')

for lines in file:
     print line

